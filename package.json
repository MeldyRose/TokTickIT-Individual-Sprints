{
  "name": "toktickit-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:server": "npm --prefix server test",
    "test:client": "npm --prefix client test",
    "test:e2e": "npm --prefix server run seed && playwright test e2e/lab-03"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
